export interface Setting {
    deltaSecond: number,
    minVal: number,
    maxVal: number,
    //for status
    standbyLimit: number,
    minDuration: number,
    //for index tabelle
    minDelta: number,
    maxDelta: number
}